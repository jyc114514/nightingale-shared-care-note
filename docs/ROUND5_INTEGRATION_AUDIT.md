# Round 5 release-candidate integration audit

Status: local integration audit in progress until the clean-clone rehearsal completes. This
document records facts and deliberate boundaries; it does not claim external CI or deployment.

## Baseline and scope

- Baseline tag: `72h-submission`, dereferenced baseline commit `573f897a69864707f64b1846b2802a2674f69597`.
- Round 5 starting branch: `codex/real-clinic-safety`.
- Round 5 starting HEAD: `2200a891ab82555a78cdef926975c75ff8108ce3`.
- Round 5 migration head: `0014_patient_publications`.
- Round 5 is integration/reproducibility work only. No new clinical feature, Voice change,
  live LLM call, RLS, external delivery, deployment, video, PDF, or ZIP work is included.

## Round-by-round inventory

| Round | Integrated surface | Data/migration | Evidence retained |
| --- | --- | --- | --- |
| Round 1 | Immutable source-anchored penicillin assertions, semantic conflict, clinician adjudication, safety floor and protected feedback | `0011_real_clinic_safety`; assertions/conflicts and projection safety fields | backend safety tests, conflict API/UI tests, source validation |
| Round 2 | Glance exposure impressions, candidate snapshot, protected conflict UI and exposure accounting | `0012_glance_impressions`; metadata-only impression batches/items | exposure tests, source/conflict E2E, warm-path evidence |
| Round 3 | PHI-safe logs, provider failure boundary, persistent circuit breaker and degraded UI | `0013_ai_provider_resilience`; circuit/retry metadata | provider resilience/log tests, Scenario E, fail-fast/warm benchmarks |
| Round 4 | Patient publication gate: draft, immutable content versions, dosage evidence, approval, publish, recall, correction | `0014_patient_publications`; three publication tables, typed projection | 44 publication tests, Scenario F, published-care benchmark |
| Round 5 | RC audit, disposable legacy probe, PostgreSQL 18 workflow, route/privacy inventory, clean-clone rehearsal | no schema/product migration added | this audit, route audit, RC evidence |

## Integration checks

The following distinctions were explicitly audited and remain separate: Accepted versus Published;
entry write CAS versus semantic clinical adjudication; risk versus display priority versus safety
floor; provider failure versus fixture fallback; exposure logging versus debiasing; internal
records versus Patient projection; portal recall versus external delivery; and local SQLite versus
real PostgreSQL evidence. The detailed route matrix is in
[`evidence/round5_route_privacy_audit.md`](evidence/round5_route_privacy_audit.md).

## Temporary-artifact hygiene

The following names were resolved under the repository and identified as generated by prior test
runs: `.pytest-tmp-round2-final`, `.pytest-tmp-round3-backend-targeted`, `.pytest-tmp-round3-final2`,
`.pytest-tmp-round3-full`, `.pytest-tmp-round3-targeted`, `.pytest-tmp-round3-targeted2` through
`.pytest-tmp-round3-targeted9`, and `.ruff-cache-round3-final`. The Ruff cache and Playwright
`frontend/test-results`/`frontend/playwright-report` directories were removable and were deleted.
The 13 pytest directories are ACL-protected; deletion returned access denied, so no ownership or
permissions were changed. `.gitignore` now excludes only `.pytest-tmp-round*/` and
`.ruff-cache-round*/`. The entire `.uv-cache` was intentionally left alone.

## Migration-chain evidence

- Fresh SQLite `0001→0014`: pass; 33 tables; publication tables present; 11 publication FKs;
  `alembic check` pass; seed twice stable; disposable DB removed.
- Legacy disposable SQLite paths `0010→0014`, `0011→0014`, `0012→0014`, `0013→0014`: all pass;
  one synthetic entry/version/comment/highlight/task per legacy probe preserved; new publication
  tables initially empty; seed twice and `alembic check` pass; every probe DB removed.
- Disposable downgrade/re-upgrade `0014→0013→0014`: pass. Publication rows intentionally fell
  from 1 to 0 because the downgrade drops the new publication tables; this is not a production
  downgrade recommendation.
- PostgreSQL offline SQL `0010→0014`: pass, 307 lines, publication/evidence tables and four
  self-reference references present, no `_alembic_tmp` token. Fresh `base→head` offline generation
  is unsupported by the existing `0003` reflection call and is recorded as such.

## PostgreSQL and external boundary

No Docker, Podman, local PostgreSQL server, `psql`, `pg_isready`, or `actionlint` was available.
Therefore no real PostgreSQL execution is claimed. The prepared workflow is
`.github/workflows/real-clinic-postgres.yml`; it uses PostgreSQL 18, Python 3.12, locked backend
dependencies, fresh/legacy-compatible migration checks, seed idempotency, schema/FK assertions,
full backend tests, Ruff, mypy, pip check, fixture AI, and Voice disabled. It has read-only
repository permissions and no production credentials or deployment step. Round 6 is the first
authorized external CI action.

## Initial bugs found and fixed

1. The stale workflow asserted `0010_postgres_compat` and omitted publication tables; it was
   replaced by the current `real-clinic-postgres.yml`.
2. The first legacy probe implementation incorrectly queried `patient_id` directly on
   `entry_versions` and publication-version tables. The probe now uses safe joins; the migration
   chain itself remained unchanged.
3. No Round 1–4 production code defect was found by the integration audit after these corrections.

## Remaining checks before RC tag

The clean-clone rehearsal and final integrated regression must complete before this audit is marked
complete and `real-clinic-rc1` is created. External PostgreSQL CI remains pending and will not be
represented as passed in Round 5.
